'use client';

import { Space_Mono } from 'next/font/google';

const spmono = Space_Mono({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export default function Leaderboard() {
  return (
    <div className="">
        Leaderboard
    </div>
  );
}
